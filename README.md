Lets see the How to Download the Dataset:

Because of the Storage problem into the Github I cant upload the Dataset CSV File into this file

Direct Dole.com/datasets/kartik2112/fraud-detectionwnload link of the dataset:- [https://www.kagg](https://www.kaggle.com/datasets/kartik2112/fraud-detection)


💳 Credit Card Fraud Detection using KNN & Decision Tree

This project demonstrates a Credit Card Fraud Detection System built using K-Nearest Neighbors (KNN) and Decision Tree Classifiers.
It includes:

A Jupyter Notebook / Python script for data preprocessing, model training & evaluation

A FastAPI backend to deploy the trained models and serve real-time predictions.

->First of all install all Install All Dependencies from requirements.txt
-> Use this command :- pip install -r requirements.txt

->then run the "Credit Card Fraud Detection using KNN & Decision Tree.ipynb" file 

-> Then Open the app.py file and the open the terminal of the VS Code and write "uvicorn app:app --reload"

-> Then put this into yout browser "http://127.0.0.1:8000/docs" and try it.
